# Vendored Provenance

Vendored files are added in the vendor task.

When files are copied into this directory, record the source repository, source path, commit hash, copy date, and any license header status here.
